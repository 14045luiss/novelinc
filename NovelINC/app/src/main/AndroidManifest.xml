<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.globalstore.novelinc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user" />
        <activity android:name=".list_review" />

        <activity
            android:name=".LayarDetail"
            android:label="Detail Trans Pembelian" />
        <activity
            android:name=".LayarListBuku"
            android:label="Daftar Novel" />
        <activity
            android:name=".LayarInsertBuku"
            android:label="Daftar Novel"/>

        <activity
            android:name=".LayarEditBuku"
            android:label="Edit Novel" />
    </application>

</manifest>